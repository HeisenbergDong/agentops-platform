# API Notes

The API is scaffolded around these domains:

- `/api/jobs`
- `/api/roles`
- `/api/rules`
- `/api/workers`
- `/api/settings`
- `/api/attachments`
- `/api/errors`

Authentication and persistence are intentionally skeletal in the first framework commit.
